function $(s){return document.querySelector(s)}function $all(s){return Array.from(document.querySelectorAll(s))}function setText(e,t){if(e)e.textContent=t}function html(e,m){if(e)e.innerHTML=m}
// Future
(function(){const form=$('#f-form'),out=$('#f-out');if(!form||!out)return;form.addEventListener('submit',async e=>{e.preventDefault();setText(out,"🔮 Summoning prophecies...");const data={name:form.name.value,birthMonth:form.birthMonth.value,favoritePlace:form.place.value};try{const res=await fetch('/api/predict-future',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const json=await res.json();setText(out,json.ok?json.content:'Error: '+(json.error||'Unknown error'));form.name.value='';form.birthMonth.selectedIndex=0;form.place.value='';}catch{setText(out,'Network error. Please try again.')}})})();
// Lie
(function(){const form=$('#gen-form'),options=$('#options'),hintEl=$('#hint'),verify=$('#verify');if(!form||!options)return;let token=null,chosen=null;form.addEventListener('submit',async e=>{e.preventDefault();const topic=e.target.topic.value;e.target.topic.value='';html(options,'<div class="pill">Generating statements...</div>');setText(hintEl,'');hintEl.style.display='none';html(verify,'');chosen=null;try{const res=await fetch('/api/lie/generate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({topic})});const json=await res.json();if(!json.ok){html(options,'Error: '+(json.error||'Unknown error'));return}token=json.token;const list=json.statements||[];html(options,'');list.forEach((s,i)=>{const d=document.createElement('div');d.className='option';d.textContent=(i+1)+'. '+s;d.onclick=()=>{chosen=i+1;$all('.option').forEach(n=>n.style.borderColor='var(--border)');d.style.borderColor='rgba(138,92,246,.6)';html(verify,'<button class="btn" id="verifyBtn">Verify</button>');$('#verifyBtn').onclick=check};options.appendChild(d)});if(json.hint){hintEl.textContent='Hint: '+json.hint;hintEl.style.display='inline-flex'}}catch{html(options,'Network error. Please try again.')}});async function check(){if(!token||!chosen)return;try{const res=await fetch('/api/lie/verify',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,guessIndex:chosen})});const json=await res.json();if(!json.ok){html(verify,'Error: '+(json.error||'Unknown error'));return}html(verify,json.correct?'<div class="pill">🎉 Correct! You found the lie.</div>':'<div class="pill">❌ Not quite. The lie was option '+json.answer+'.</div>')}catch{html(verify,'Network error. Please try again.')}}})();
// Character (conversational)
(function(){const startForm=$('#start-form'),turnForm=$('#turn-form'),chat=$('#chat'),rounds=$('#rounds'),result=$('#result'),answerBox=$('#answer-box');if(!startForm||!chat)return;let sessionId=null,roundsLeft=10;function pushMsg(who,text){const d=document.createElement('div');d.className='msg';d.innerHTML='<b>'+who+':</b> '+text;chat.appendChild(d);chat.scrollTop=chat.scrollHeight}
startForm.addEventListener('submit',async e=>{e.preventDefault();html(chat,'<div class="pill">Picking a secret character...</div>');const topic=e.target.topic.value;e.target.topic.value='';try{const res=await fetch('/api/character/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({topic})});const json=await res.json();if(!json.ok){html(chat,'Error: '+(json.error||'Unknown error'));return}sessionId=json.sessionId;roundsLeft=10;$('#game').style.display='block';html(chat,'');pushMsg('AI',json.message||'Ask your first yes/no question!');setText(rounds,'Rounds left: '+roundsLeft)}catch{html(chat,'Network error. Please try again.')}});
if(turnForm){turnForm.addEventListener('submit',async e=>{e.preventDefault();if(!sessionId)return;const line=$('#userline').value.trim();if(!line)return;pushMsg('You',line);$('#userline').value='';try{const res=await fetch('/api/character/turn',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({sessionId,text:line})});const json=await res.json();if(!json.ok){pushMsg('AI','Error: '+(json.error||'Unknown error'));return}if(json.answer){pushMsg('AI',json.answer);if(answerBox){answerBox.textContent=json.answer}}if(json.hint){pushMsg('AI','💡 Hint: '+json.hint)}if(json.done){if(json.win){html(result,'<div class="pill">🎉 Correct! You found '+json.name+'.</div>')}else{html(result,'<div class="pill">🕵️ Out of rounds. The character was '+json.name+'.</div>')}sessionId=null;if(json.message)pushMsg('AI',json.message);return}if(typeof json.roundsLeft==='number'){roundsLeft=json.roundsLeft;setText(rounds,'Rounds left: '+roundsLeft)}}catch{pushMsg('AI','Network error. Please try again.')}})}})();
